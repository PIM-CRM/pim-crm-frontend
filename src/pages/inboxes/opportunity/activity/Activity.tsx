import ActivityTimeline from '../../../profile/activity/ActivityTimeline';

const Activity = () => {
      return (
            <div>
                  <ActivityTimeline />
            </div>
      );
};

export default Activity;
